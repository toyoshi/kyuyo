<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "給与明細.net - 無料の給与支払明細書PDF作成サービス" %></title>
    <meta name="description" content="給与明細.netは登録不要ですぐにオンラインで給与明細（給与支払明細書）が作成できるサービスです">
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->
    <style type="text/css">
      body {
        padding-top: 20px;
        padding-bottom: 40px;
      }

      /* Custom container */
      .container-narrow {
        margin: 0 auto;
        max-width: 700px;
      }
      .container-narrow > hr {
        margin: 30px 0;
      }

      /* Main marketing message and sign up button */
      .jumbotron {
        margin: 60px 0;
        text-align: center;
      }
      .jumbotron h1 {
        font-size: 72px;
        line-height: 1;
      }
      .jumbotron .btn {
        font-size: 21px;
        padding: 14px 24px;
      }

      /* Supporting marketing content */
      .marketing {
        margin: 60px 0;
      }
      .marketing p + h4 {
        margin-top: 28px;
      }
    </style>

    <%= stylesheet_link_tag "application", :media => "all" %>
  </head>
  <body>
    <div class="container-narrow">

      <div class="masthead">
        <ul class="nav nav-pills pull-right">
          <li class="active"><%= link_to "新規作成", new_payslip_path  %></li>
        </ul>
          <h3 class="muted">
            <%= link_to '給与明細.net', root_path, class:'brand' %>
          </h3>
      </div>

      <hr>

      <div class="jumbotron">
        <h1>給与明細.net</h1>
        <p class="lead">登録不要！いますぐ簡単に給与明細を作れるサイトです</p>
      </div>

      <hr>

      <div class="row-fluid marketing">
          <%= bootstrap_flash %>
          <%= yield %>
      </div><!--/row-->


      <footer>
      <p>&copy; <a href="http://twitter.com/toyoshi/">@toyoshi</a> 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
